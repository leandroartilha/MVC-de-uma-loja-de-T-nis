@model ListarTenisViewModel

<div>
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container">
    <div class="row">
        @foreach (var tenis in Model.Tenis)
        {
            <partial name="_TenisResumo" for="@tenis" />
        }
    </div>
</div>




